import { UpdateAddressRequest } from './update-address-request';

describe('UpdateAddressRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateAddressRequest()).toBeTruthy();
  });
});
