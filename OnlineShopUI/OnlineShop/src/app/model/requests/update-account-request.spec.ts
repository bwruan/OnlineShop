import { UpdateAccountRequest } from './update-account-request';

describe('UpdateAccountRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateAccountRequest()).toBeTruthy();
  });
});
