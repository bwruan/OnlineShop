import { PurchaseOrderRequest } from './purchase-order-request';

describe('PurchaseOrderRequest', () => {
  it('should create an instance', () => {
    expect(new PurchaseOrderRequest()).toBeTruthy();
  });
});
