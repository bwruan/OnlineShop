import { AddAddressRequest } from './add-address-request';

describe('AddAddressRequest', () => {
  it('should create an instance', () => {
    expect(new AddAddressRequest()).toBeTruthy();
  });
});
