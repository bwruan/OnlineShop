import { AddAccountRequest } from './add-account-request';

describe('AddAccountRequest', () => {
  it('should create an instance', () => {
    expect(new AddAccountRequest()).toBeTruthy();
  });
});
